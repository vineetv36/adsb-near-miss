# Host-side Python dependencies (outside Docker)
# Install with:  pip install -r requirements.txt
# Or via Poetry: poetry install

# ── Kafka ─────────────────────────────────────────────────────────────────────
confluent-kafka>=2.3
kafka-python>=2.0

# ── Geospatial / analysis ─────────────────────────────────────────────────────
numpy>=1.26
shapely>=2.0
hdbscan>=0.8

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.109
uvicorn[standard]>=0.27
asyncpg>=0.29
redis[asyncio]>=5.0
pydantic>=2.5

# ── Ingestion helpers ─────────────────────────────────────────────────────────
python-geohash>=0.8
httpx>=0.27

# ── Notebook ──────────────────────────────────────────────────────────────────
jupyter>=1.0
matplotlib>=3.8
pandas>=2.1
folium>=0.15

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
